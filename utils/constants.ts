export enum Environment {
  DEVELOPMENT = "dev",
  PRODUCTION = "prod",
}

export enum MoneyType {
  INCOME = "income",
  EXPENSE = "expense",
}

export enum PaymentType {
  FIXED = "fixed",
  OVER_TIME = "over_time",
  CURRENT = "current",
}
